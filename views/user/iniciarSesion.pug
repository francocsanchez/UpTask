doctype html
html(lang="en")
    head
        include ../partials/head
    body.login
        .contenedor-formulario 
            h1 Iniciar sesión en UpTask

            if locals.mensajes
                .alertas 
                    each categoria in Object.keys(locals.mensajes)
                        each error in mensajes[categoria]
                            .alerta(class=`${categoria}`)
                                p= error

            form#formulario.caja.caja-login(method='POST' action='/iniciar-sesion')
                .campo 
                    label(for="email") Email: 
                    input(type="email" name="email" placeholder="Ingrese un email" value= email) 
                .campo 
                    label(for="password") Password: 
                    input(type="password" name="password" placeholder="Ingrese una contraseña" value= password)
                .campo.enviar 
                    input.boton(type="submit" value="Crear cuenta")
                .campo 
                    a(href="/crear-cuenta") ¿ No tenes cuenta ? Crea una gratis