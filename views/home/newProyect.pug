extends ../layout

include ../mixins/formProyect

block content
    .contenido-principal
        h1 #{title}

        if errors 
            each error in errors 
                .alerta.error= error.texto
        +formProyect(proyect)
